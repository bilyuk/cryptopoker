# NestJS API framework

The API server will use NestJS for HTTP routes and realtime gateways. This is more structured than a lean Fastify server, but the module system, dependency injection, guards, and gateway abstractions give the authoritative poker server clearer boundaries as room lifecycle, host approvals, WebSocket events, and game state grow.
