# Host-verified buy-ins before in-app escrow

Version 1 will use dollar-denominated table chips without moving or holding funds in the app. A Player requests a Buy-In, the Room Host verifies outside-the-app payment, and the API creates a Table Stack only after that approval; this keeps the poker flow realistic while deferring true escrow and wallet mechanics to a later design.
